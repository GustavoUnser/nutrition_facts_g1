import React from "react"
import { Text } from "react-native"

const Loading = () => {
    return(
        <Text>Please wait. Your food is beeing searched...</Text>
    )
}

export default Loading